{
  "name": "generative-ui-playground",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.14.0",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
